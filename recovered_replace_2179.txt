{
  "AllowMultiple": false,
  "Description": "Updating Completeness score logic",
  "EndLine": 149,
  "Instruction": "Rewrite the completeness score calculation to include the new JSON fields like universal_deliverables, assets, project_details, etc. and score them properly up to 100%.",
  "ReplacementContent": "      // Calculate Completeness\n      let score = 0;\n      let itemsChecked = 0;\n      \n      const hasClient = !!updated.client_details?.client_name;\n      if (hasClient) { score += 15; itemsChecked++; }\n      \n      const hasProject = !!(updated.project_details?.project_name && updated.objective);\n      if (hasProject) { score += 15; itemsChecked++; }\n      \n      const hasTimeline = !!updated.timeline?.delivery_date;\n      if (hasTimeline) { score += 10; itemsChecked++; }\n\n      const hasCategory = !!updated.project_details?.project_category;\n      if (hasCategory) { score += 10; itemsChecked++; }\n\n      const hasProdType = !!updated.project_details?.production_type;\n      if (hasProdType) { score += 10; itemsChecked++; }\n      \n      const hasAssets = !!(updated.assets?.brand_guidelines || updated.assets?.reference_links || updated.assets?.drive_link);\n      if (hasAssets) { score += 15; itemsChecked++; }\n      \n      const hasDeliverables = !!(updated.universal_deliverables?.formats?.length || updated.universal_deliverables?.output_ratios?.length);\n      if (hasDeliverables) { score += 15; itemsChecked++; }\n      \n      let hasWorkflowDetails = false;\n      const prodType = updated.project_details?.production_type;\n      if (prodType === 'ai') hasWorkflowDetails = !!updated.ai_style?.visual_style;\n      else if (prodType === 'live') hasWorkflowDetails = !!updated.live_shoot_details?.locations;\n      else if (prodType === 'hybrid') hasWorkflowDetails = !!updated.hybrid_details?.ai_components?.length;\n      else if (prodType === 'photo') hasWorkflowDetails = !!updated.photography_details?.shoot_type;\n      else if (prodType === 'post') hasWorkflowDetails = !!updated.post_production_details?.editing_style;\n      else hasWorkflowDetails = false;\n      \n      if (hasWorkflowDetails) { score += 10; itemsChecked++; }\n\n      updated.completeness_score = Math.min(100, score);\n      updated.items_checked = itemsChecked;",
  "StartLine": 125,
  "TargetContent": "      // Calculate Completeness\n      let score = 0;\n      let itemsChecked = 0;\n      \n      const hasClient = updated.client_details?.client_name;\n      if (hasClient) { score += 15; itemsChecked++; }\n      \n      const hasProject = updated.project_details?.project_name && updated.objective;\n      if (hasProject) { score += 15; itemsChecked++; }\n      \n      const hasTimeline = updated.timeline?.delivery_date;\n      if (hasTimeline) { score += 15; itemsChecked++; }\n\n      const hasCategory = updated.project_details?.project_category;\n      if (hasCategory) { score += 15; itemsChecked++; }\n\n      const hasProdType = updated.project_details?.production_type;\n      if (hasProdType) { score += 15; itemsChecked++; }\n      \n      const hasAssets = updated.assets?.brand_guidelines || updated.assets?.reference_links;\n      if (hasAssets) { score += 25; itemsChecked++; }\n      \n      updated.completeness_score = Math.min(100, score);\n      updated.items_checked = itemsChecked; ",
  "TargetFile": "d:\\Workstation DP-2\\DP Clients\\WEB DEVELOPMENT\\Media OS V.1\\src\\app\\(dashboard)\\crm\\[prospectId]\\components\\RequirementChartForm.tsx",
  "toolAction": "Updating completeness logic",
  "toolSummary": "File edit"
}