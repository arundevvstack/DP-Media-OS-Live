import { NextResponse } from "next/server";
import { userRepository } from "@/domains/identity/repositories/UserRepository";

export class UsersApiService {
    static async handleGET(request: Request) {
    }
}