// ==========================================
// MISSING HR MODELS (To fix runtime crash)
// ==========================================

model OrganizationUnit {
  id         String   @id @default(uuid())
  company_id String
  name       String
  type       String   @default("DEPARTMENT")
  is_active  Boolean  @default(true)
  created_at DateTime @default(now())

  Users User[]
}
